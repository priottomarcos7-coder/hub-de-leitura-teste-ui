pipeline {
    agent any

    stages {
        stage('Instalar dependências') {
            steps {
                bat 'npm install'
            }
        }

        stage('Rodar Cypress') {
            steps {
                bat 'npx cypress run'
            }
        }
    }
}